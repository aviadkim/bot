{
  "name": "customer-service-chatbot",
  "version": "1.0.0",
  "description": "Customer service chatbot with React frontend and Express backend",
  "scripts": {
    "clean": "rm -rf */node_modules node_modules backend/public frontend/build",
    "setup": "npm run clean && npm install",
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && mkdir -p public && cp -r ../frontend/build/* public/",
    "start": "cd backend && node server.js"
  },
  "dependencies": {
    "concurrently": "^8.0.1",
    "node-fetch": "^3.3.2",
    "rimraf": "^5.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
