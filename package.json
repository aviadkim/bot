{
  "name": "customer-service-chatbot",
  "version": "1.0.0",
  "description": "Full-stack customer service chatbot application",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build": "npm install && cd frontend && npm install && npm run build && cd .. && rimraf backend/public && mkdir backend/public && xcopy /E /I frontend\build backend\public",
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd frontend && npm start\" \"cd backend && npm run dev\""
  },
  "dependencies": {
    "rimraf": "^5.0.1",
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}