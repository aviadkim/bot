{
  "name": "customer-service-chatbot",
  "version": "1.0.0",
  "description": "Customer service chatbot with React frontend and Express backend",
  "scripts": {
    "install": "NODE_ENV=production cd backend && npm install --omit=dev",
    "prebuild": "rm -rf backend/public || true",
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install --omit=dev && mkdir -p public && rm -rf public/* && cp -r ../frontend/build/* public/",
    "start": "cd backend && NODE_ENV=production node server.js",
    "clean": "rm -rf backend/public frontend/build node_modules */node_modules"
  },
  "dependencies": {
    "concurrently": "^8.0.1",
    "node-fetch": "^3.3.2",
    "rimraf": "^5.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
