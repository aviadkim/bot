{
  "name": "customer-service-chatbot",
  "version": "1.0.0",
  "description": "Customer service chatbot with React frontend and Express backend",
  "scripts": {
    "install": "cd backend && npm install --production",
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install --production && mkdir -p public && rm -rf public/* && cp -r ../frontend/build/* public/",
    "start": "cd backend && node server.js",
    "clean": "rm -rf frontend/build backend/public node_modules frontend/node_modules backend/node_modules"
  },
  "dependencies": {
    "concurrently": "^8.0.1",
    "node-fetch": "^3.3.2",
    "rimraf": "^5.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
